#include "main.h"

/**
 * int _abs - Declaration of function for compute the absolute value
 *
 * Return: 0 (Success)
 *
 */

int abs(int)

int number;

{
	if (number < 0)

{
	number = number * (-1);
	printf("%d", number);
}
	else
	{
	printf("%d", number)
	}
	return (0);
}
