#include "main.h"

/**
 * int _abs - Declaration of function for compute the absolute value
 *
 * Return: 0 (Success)
 *
 */

int abs(int)

int number;

{
if (number >= 0; abs;)
{
else (number <= 0; number =!; abs;)
}
return (0);
}
