#include "main.h"

/**
 * print_last_digit - Declaration of last digit of number
 * @???: ????
 * Return: 0 (Success)
 */

int print_last_digit(int);

{
	int digit;
	int number;

	if (number = digit % 10)
		{
		return (number);
		}
	return (0);
}
