#include "main.h"

/**
 * int _islower(int c) - int _islower(int c) is for check a lowercase letters
 *
 * Return: 0 (Sucess)
 *
 */

int _islower(int c)

int c;
{
{
if (c = 'a', c <= 'z', c++)
return (1);
}
{
else
return (0);
}
}
