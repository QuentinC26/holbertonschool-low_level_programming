#include "main.h"

/**
 * int _islower(int c) - int _islower(int c) is used for checking a lowercase letters
 *
 * Return: 0 (Sucess)
 *
 */

void print(void)

char = lowercase;

if (lowercase = 'a'; lowercase <= 'z' return 0; lowercase++)
if (lowercase = 'c'; loercase = return 1; lowercase++)
_putchar (lowercase);
