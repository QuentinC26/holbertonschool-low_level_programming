#include "main.h"

/**
 * _islower - Check the lowercase letters.
 * @c: 'c' is a variable
 *
 * Return: 0 (Sucess)
 *
 */

int _islower(int c)

{
if (c >= 'a' && c <= 'z')
return (1);
else
return (0);
}
