#include "main.h"

/**
 * void jack_bauer - Functions for print every minutes of day of Jack Bauer
 * @?: ??
 * Return: 0 (Success)
 */

void jack_bauer(void)

{
	int min;

	for (min = 0; min < 2; min++)
	for (min = 0; min < 3; min++)
	_putchar(':');
	for (min = 0; min < 5; min++)
	for (min = 0; min < 9, min++)
	_putchar( min + '0');
}
