#include "main.h"

/**
 * swap_int - Function for swap the value of two integers
 * @,: Description a mettre
 * Return: 1 (Error)
 */

void swap_int(int *a, int *b)

{

	replacement;
	
	remplacement = a;
	a = b;
	b = remplacement;

	_putchar(a + '0');
	_putchar(b + '0');
}
