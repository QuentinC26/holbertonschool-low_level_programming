#include "main.h"

/**
 * _strlen: Function for return the length of a string
 * @?: ?
 * Return: 0 (Success)
 */

int _strlen(char *s)

{

	char *s [*s] = "My first strlen!";
	int length;

	for (*s = lenght ; *s < lenght ; *s++)
	return (*s);
}
