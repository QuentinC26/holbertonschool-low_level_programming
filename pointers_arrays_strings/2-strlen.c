#include "main.h"

/**
 * _strlen: Function for return the length of a string
 * @?: ?
 * Return: 0 (Success)
 */

int _strlen(char *s)

{
	int length;

	for (*s = 0 ; *s <= /0; *s++)
	return (*s);
}
