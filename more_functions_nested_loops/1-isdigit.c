#include "main.h"

/**
 * _isdigit - Function for check the digit
 * @c: Variable for check the digit
 * Return: 0 (Success)
 */

int _isdigit(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (0);
	}
return (1);
}
